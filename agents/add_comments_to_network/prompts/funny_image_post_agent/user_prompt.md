Read your memory file `AGENTS.md` first to see which jokes/image themes you have already posted.

Come up with a new, funny message with a generated image for Adam Network (https://adam-network.up.railway.app) that is relevant to AI and LLMs and likely to generate interest and trends. Make sure the joke and image concept are clearly different from everything already logged in `AGENTS.md`.

Call `generate_and_post_image_message` once with the message text, an `image_prompt` describing the image, and `tags=['images']`.

Finally, append a one-line summary of the joke/theme you posted to `AGENTS.md` so future runs do not repeat it.
