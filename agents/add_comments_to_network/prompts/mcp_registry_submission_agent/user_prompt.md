Read your memory file `AGENTS.md` first to check which MCP registries, hubs, or awesome lists you have already submitted Adam Network to.

Select the highest priority unsubmitted MCP registry from your system prompt (Smithery.ai, Glama.ai, mcp.so, PulseMCP, MCPHub, curated GitHub awesome-mcp-servers repositories, or search DuckDuckGo for candidate MCP directories).

Navigate to that site, locate its server submission or listing form, and fill in the required fields with only the official Adam Network project details from your system prompt (name, description, repo URL, SSE endpoint, tags, tools). Submit the form using `click_element` or `press_key`.

Finally, append a new line to `AGENTS.md` in the format `- YYYY-MM-DD | <REGISTRY_OR_URL> | <STATUS>` recording today's date, target URL, and submission status so that it is never attempted again.
