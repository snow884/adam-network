Read your memory file `AGENTS.md` to see which GitHub repositories you have already scanned and targeted.

Search GitHub for an open-source AI agent project (using MCP, LangChain, AutoGen, CrewAI, LlamaIndex, or custom Python agent frameworks) that is NOT listed in `AGENTS.md`.

Inspect the chosen repository's files and dependencies to determine its framework, then generate a tailored Adam Network integration example and PR proposal.

Submit the Pull Request (or Issue/Discussion) with the working code example, and record the repository, date, and link in `AGENTS.md`.
